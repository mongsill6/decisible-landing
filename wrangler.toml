name = "decisible"
compatibility_date = "2024-01-01"
pages_build_output_dir = "dist"

# Secrets are set via: npx wrangler secret put ANTHROPIC_API_KEY
# Local dev: use .dev.vars file

# KV Namespace for Rate Limiting
# Production: Set binding in Cloudflare Dashboard > Pages > decisible > Settings > Functions > KV namespace bindings
# Local dev: Create KV with `npx wrangler kv namespace create RATE_LIMIT_KV` and paste the id below
[[kv_namespaces]]
binding = "RATE_LIMIT_KV"
id = "d435ffdd8b5845c18f0130bf5644bff5"
